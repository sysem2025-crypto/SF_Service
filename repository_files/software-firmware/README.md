# Software and Firmware Directory

Store here the real operational packages used by the `Firmware & Software` page, for example:

- firmware binaries
- desktop tools
- ZIP archives
- update packages
- release notes

Suggested naming style:

`YYYYMMDD_Product_PackageName_Version.ext`
