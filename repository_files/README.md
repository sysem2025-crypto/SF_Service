# Repository Files

This area stores the real downloadable assets referenced by the web portal.

- `documentation/`: manuals, procedures, checklists, wiring notes, PDFs, DOCX
- `software-firmware/`: firmware packages, installers, technical tools, ZIP archives

Keep the metadata in `data/` and the real files here.
